<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wjh.mapper.ShopOrderMapper">
    <select id="getByUserId" resultType="com.wjh.vo.ShopOrderVO">
        select so.*, s.name as shopName, v.value as voucherValue
        from shop_order so
        join shop s on so.shop_id = s.id
        left join voucher v on so.voucher_id = v.id
        where so.user_id = #{userId}
        and so.status = #{status}
        order by so.order_time desc
    </select>
</mapper>